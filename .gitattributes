# Automatically normalize line endings for text files
* text=auto

# Ensure Dart files use LF (Unix-style) line endings
*.dart text eol=lf

# Ensure YAML files use LF (Unix-style) line endings
*.yaml text eol=lf

# Ensure JSON files use LF (Unix-style) line endings
*.json text eol=lf

# Ensure Markdown files use LF (Unix-style) line endings
*.md text eol=lf

# Ensure Shell scripts use LF (Unix-style) line endings
*.sh text eol=lf

# Ensure Batch scripts use CRLF (Windows-style) line endings
*.bat text eol=crlf

# Treat binary files as binary (no line ending conversion)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.mov binary
*.mp4 binary
*.zip binary
*.jar binary

# Dart package manager files
.pubignore text eol=lf

# Flutter specific files
*.dart_tool text eol=lf
*.flutter-plugins text eol=lf
*.flutter-plugins-dependencies text eol=lf
*.packages text eol=lf

# Other text files
*.txt text eol=lf
*.xml text eol=lf
*.html text eol=lf
*.css text eol=lf
*.js text eol=lf
*.ts text eol=lf
*.htm text eol=lf
